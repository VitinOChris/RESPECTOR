import { makeStyles } from '@material-ui/styles'

const styles = makeStyles(() => ({
      drawer: {
        width: 240,
        flexShrink: 0,
      },
      drawerPaper: {
        width: 243,
    },
      drawerContainer: {
        overflow: 'auto',
      },
    typograf: {
        padding: 10,
        marginLeft: 20
    },
    typograf2: {
        padding: 10,
        marginLeft: 65
    },
    categori: {
        padding: 10
    },
    categori2: {
        padding: 10
    }
}));

export default styles;
